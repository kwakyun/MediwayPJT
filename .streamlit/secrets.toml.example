# Copy this file to `.streamlit/secrets.toml` and enter your own keys.
# The app still runs in stored-data/basic-CS mode when keys are omitted.

ODSAY_API_KEY = ""
GEMINI_API_KEY = ""
GEMINI_MODEL = "gemini-3.5-flash"
